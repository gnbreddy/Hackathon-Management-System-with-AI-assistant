version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: ehub-postgres
    environment:
      - POSTGRES_USER=ehub_user
      - POSTGRES_PASSWORD=ehub_password
      - POSTGRES_DB=ehub_db
    ports:
      - "5433:5432"
    restart: always